{% if template == '404' %}
  <script>
    fetch('/apps/error-insights/insights/update-error-insights', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        url: window.location.href,
        referrer: document.referrer,
        timestamp: new Date().toISOString(),
      }),
    });
  </script>
{% endif %}

{% schema %}
{
  "name": "SEO app",
  "target": "head"
}
{% endschema %}
